const nextConfig = {
  i18n: {
    locales: ['en', 'hi'],
    defaultLocale: 'en'
  },
  reactStrictMode: true,
  images: {
    domains: ['cdn.dummyjson.com', 'freepik.com', 'equalengineers.com'],
  },
};

export default nextConfig;
